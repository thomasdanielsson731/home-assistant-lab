# AI Insights

Enrichment, correlation, and natural-language understanding over the event model.

---

## Pipeline

```
Events (raw)
    ↓
Enrichment (LLM caption, correlation)
    ↓
Insights (patterns, anomalies, summaries)
    ↓
Output (timeline text, weekly report, NL query answer)
```

```
Collect → Enrich → Analyze → Visualize → Understand
```

---

## Enrichment Types

| Enrichment | Input | Output | Model |
|---|---|---|---|
| Scene caption | Event snapshot | `ai_summary` text | Qwen-VL / LLaVA (Ollama) |
| Event correlation | Event batch (same window) | `metadata.correlations` | Rule engine + LLM |
| Anomaly flag | Daily aggregate vs baseline | `insight.anomaly: true` | Statistical + LLM |
| Weekly summary | 7-day aggregates | Markdown report | Qwen (Ollama) |

---

## Enriched Event Example

```json
{
  "event_id": "evt_20260606_181200_driveway_bicycle",
  "type": "bicycle",
  "ai_summary": "A person arrived at the driveway gate on a bicycle. The front door was unlocked 45 seconds later.",
  "enriched": true,
  "metadata": {
    "correlations": [
      { "event_id": "evt_..._door", "type": "door", "offset_seconds": 45 }
    ]
  }
}
```

---

## NL Query Interface (Phase 8 — Digital Twin)

User asks → LLM queries event store + aggregates → natural language answer.

| Question | Data needed |
|---|---|
| "What happened today?" | Today's events → timeline summary |
| "Why was energy high this week?" | Energy aggregates + outdoor activity correlation |
| "When does the black cat usually visit?" | Cat events → time-of-day histogram |
| "How many bicycle trips this week?" | Bicycle events → weekly count |
| "Is anyone outside?" | `binary_sensor.house_outdoor_presence` + recent person events |

Context window: last 24 h events JSON + daily aggregates + house state template sensors.

---

## Anomaly Detection

Baseline: mean event rate per (zone, type, hour-of-day, weekday) over 14 days.

Alert when current rate > 2σ above baseline.

| Anomaly | Example |
|---|---|
| Unusual person at 03:00 | `person` at `front` outside normal hours |
| Cat visit spike | 5 cat events in one day vs avg 1 |
| CO₂ jump | environment event with CO₂ > 2σ |

Output: `insight` event type (meta-event) or HA notification.

---

## Weekly Report (agent-generated)

```markdown
## Danielsson Insights · Week 23

**Activity:** 47 person events, 12 vehicle, 4 bicycle trips, 3 cat visits.

**People:** Nils cycled home 5 times (weekdays ~15:30). Thomas arrived by car 3 times.

**Cats:** Black cat visited backyard 3 times, usually evening.

**Environment:** Avg outdoor temp 16°C. CO₂ stable around 430 ppm.

**Notable:** Unknown person at front door Tuesday 22:14.
```

Generated by Cursor agent or Ollama script reading event exports.

---

## Infrastructure

| Component | Host | Phase |
|---|---|---|
| Ollama + Qwen | Windows dev PC | 6 |
| Vision model (Qwen-VL) | Windows dev PC | 6 |
| Enrichment worker | Dev PC (polls event store) | 6 |
| HA Assist → Ollama | HAOS → dev PC REST | 6 |

All local. No cloud API for security data.

---

## Cursor Agent Role

Use [agents/analyst.md](../../agents/analyst.md) for insight queries.

Prompt pattern:

> Given the event model in `docs/analytics/event-model.md`, analyze [question] using events from [date range]. Output a summary and suggest dashboard changes if patterns are significant.
