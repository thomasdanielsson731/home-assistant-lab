- id: frigate_person_detected
  alias: "Frigate — person vid entré (enkel)"
  initial_state: false  # Av — använd notify_person_entry_multisensor i smart_notifications.yaml
  description: >
    Enkel Frigate-push utan AOA/scene-bekräftelse. Multisensor-varianten är på som standard.
  trigger:
    - platform: mqtt
      topic: "frigate/events"
  condition:
    - condition: template
      value_template: >
        {{ trigger.payload_json.type == 'new'
           and trigger.payload_json.after.label == 'person'
           and trigger.payload_json.after.camera in ['front', 'driveway_id', 'driveway_wide'] }}
  action:
    - service: notify.mobile_app_thomas_iphone_15
      data:
        title: "Person detected"
        message: "{{ trigger.payload_json.after.camera | replace('_', ' ') | title }}"
        data:
          image: /api/frigate/notifications/{{ trigger.payload_json.after.id }}/snapshot.jpg
  mode: single
  max_exceeded: silent
