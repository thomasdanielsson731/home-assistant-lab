# Environment metrics — full-screen iframe (mirrors house-timeline / Analytics)
# Served from Windows dev PC: scripts/timeline_server.py :8765/environment
views:
  - title: Environment
    panel: true
    cards:
      - type: iframe
        url: !secret environment_url
        aspect_ratio: 100%
